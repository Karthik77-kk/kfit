<?xml version="1.0" encoding="utf-8"?>
<!--
    Single ImageView: KFitnessWidgetProvider draws the entire widget
    to a Canvas bitmap for full design control (rings, metrics,
    progress bars, header, insight strip — no RemoteViews colour limits).
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_canvas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:contentDescription="K Fitness widget"/>

</FrameLayout>
